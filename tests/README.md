# Tests

This folder is reserved for integration tests (Anchor TS tests or Rust program tests).

Current environment note: `anchor-cli` is not installed in this session, so tests are scaffolded
but not yet executable here.

