[package]
name = "realms-adapter"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "realms_adapter"

[features]
no-entrypoint = []
default = []

[dependencies]
solana-program = { workspace = true }
borsh = { workspace = true }
